<!DOCTYPE html>
<html>
    <head>
        <title>Cursos</title>

        <link 
            rel="stylesheet" 
            href="https://cdn.jsdelivr.net/npm/bootstrap@3.3.7/dist/css/bootstrap.min.css" 
        />

    </head>

    <body>
        <div class="container">
            <form action="/atualizar/<%= id %>" method="post">
                <div class="form-group">
                    <label>Descrição</label>
                    <input type="text" class="form-control" name="descricao" value="<%= descricao %>" placeholder="Descrição">
                </div>
                <div class="form-group">
                    <label>Carga horária</label>
                    <input type="text" class="form-control" name="carga_horaria" value="<%= carga_horaria %>" placeholder="Carga horária">
                </div>
                <button type="submit" class="btn btn-info">Atualizar</button>
            </form>
        </div>
    </body>
</html>